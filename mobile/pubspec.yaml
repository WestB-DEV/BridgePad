name: bridgepad
description: "An explicitly armed phone-to-Flipper USB keyboard and mouse bridge."
publish_to: 'none'
version: 0.2.0+7

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: 5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
